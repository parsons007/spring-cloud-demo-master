spring.application.name: config-server
server.port: 8888

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/parsons007/config-server
          searchPaths: /**
          #username: your name
          #password: your pass
      label: master

